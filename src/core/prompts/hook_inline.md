# Skill recommendations (runai recommend)

**Action required**: (1) start your reply with one short line `激活 skill: {NAME}` so the user sees runai routing worked; (2) treat the SKILL.md content below as already-read — DO NOT Read the file path, the full body is inlined; (3) act on the instructions directly.

**This skill is already loaded for this turn via runai's UserPromptSubmit hook.** Do NOT call `sm_enable` / `sm_install` / `runai enable` / `runai install` / any "activate" or "install" tool — the SKILL.md content below IS the activation. Even if `sm_list` shows the skill as "disabled" for the target CLI, that only affects future sessions; for this turn it is fully usable. Just follow the instructions.

## Activated skill: {NAME}
_Source path (FYI only, do not Read): `{PATH}`_
_Full SKILL.md content follows — treat this section as if you just read the file:_

{CONTENT}
