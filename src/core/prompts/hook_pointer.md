# Skill recommendations (runai recommend)

**Action required**: (1) start your reply with one short line `激活 skill: {NAME}` so the user sees runai routing worked; (2) the SKILL.md is too large to inline within Claude Code's 10 KB hook output cap — Read it ONCE at the path below; (3) do not re-Read it later.

**This skill is being loaded for this turn via runai's UserPromptSubmit hook.** After Reading the SKILL.md once, just act on its instructions. Do NOT call `sm_enable` / `sm_install` / `runai enable` / `runai install` / any "activate" or "install" tool — Reading the SKILL.md at the path below IS the activation. Even if `sm_list` shows the skill as "disabled" for this target CLI, that only affects future sessions; the SKILL.md you Read is fully usable this turn.

Activated skill: **{NAME}**
SKILL.md path: `{PATH}`
What it does: {DESC}
